export const config={matcher:['/','/(pricing|done-for-you)']};export default async function middleware(request){const p=new URL(request.url).pathname,ua=request.headers.get('user-agent')||'';if(p==='/'&&/Ralf-Staging-V2-Direct/i.test(ua))return fetch(new URL('/api/source-home',request.url),{headers:{'x-ralf-marketing-renderer':'1'}});const renderer=p==='/'?'/api/hero-v2-base':p.includes('/done-for-you')?'/api/done-for-you':'/api/pricing-base';const r=await fetch(new URL(renderer,request.url),{headers:{'x-ralf-marketing-renderer':'1'}});if(p.includes('/done-for-you'))return r;let html=await r.text();html=html.replace(/STAGING · HERO V2 · GREEN ACCENTS/g,'');const headers=new Headers(r.headers);headers.delete('x-robots-tag');headers.delete('x-ralf-staging');headers.set('content-type','text/html; charset=utf-8');headers.set('cache-control','public, s-maxage=300, stale-while-revalidate=86400');return new Response(html,{status:r.status,headers});}