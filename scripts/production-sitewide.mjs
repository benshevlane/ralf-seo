import { promises as fs } from 'node:fs';import path from 'node:path';const root=process.cwd(),skip=new Set(['.git','node_modules','.vercel','public']);const tag='<link rel="stylesheet" href="/assets/staging-emerald.css" data-ralf-emerald>';async function walk(dir){for(const e of await fs.readdir(dir,{withFileTypes:true})){if(e.isDirectory()&&skip.has(e.name))continue;const f=path.join(dir,e.name);if(e.isDirectory()){await walk(f);continue;}if(!e.isFile()||!e.name.endsWith('.html'))continue;let h=await fs.readFile(f,'utf8');if(!h.includes('data-ralf-emerald')&&/<\/head>/i.test(h)){h=h.replace(/<\/head>/i,tag+'\n</head>');await fs.writeFile(f,h);}}}await walk(root);console.log('Emerald marketing styles injected.');